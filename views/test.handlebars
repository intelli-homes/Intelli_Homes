<img id="myImg" src="images/cam1.jpg" />

<video id="myVideo" src="media/example.mp4" />
<canvas id="myCanvas" />


<script>
    const input = document.getElementById('myImg')
    const canvas = require('canvas')
const faceapi = require('face-api.js')

let detections
async function detectface() {

    detections = await faceapi.detectAllFaces(input)
    return detections 
}

detectface()
function df() {
    return detections
}
df()

async function detecthighconfidface() {

    const detection = await faceapi.detectSingleFace(input)
    return detection
}
detecthighconfidface()

function dhcf() {
    return detecthighconfidface()
}

dhcf()

</script>